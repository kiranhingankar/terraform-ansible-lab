[defaults]
inventory = inventory.ini
host_key_checking = False
private_key_file = ~/.ssh/terra-key-ansible

; remote_user = ubuntu
retry_files_enabled = False
timeout = 30

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False